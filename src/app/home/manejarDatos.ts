export interface ManejarDatos {
    id: number, // al iniciarse no tiene un id, por eso el ?, porque puede no ir
    categoria: string,
    titulo: string,
    fechaInicio: string,
    fechaFin: string,
    info: string,
}